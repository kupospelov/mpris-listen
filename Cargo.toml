[package]
name = "mpris-listen"
version = "0.1.0"
authors = ["Konstantin Pospelov <kupospelov@gmail.com>"]
edition = "2018"

[dependencies]
dbus = "0.6"
serde = { version = "1.0", features = ["derive"] }
toml = "0.5"
